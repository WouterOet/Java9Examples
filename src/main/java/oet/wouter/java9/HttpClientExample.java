package oet.wouter.java9;

import java.net.URI;
//import java.net.http.HttpClient;
//import java.net.http.HttpResponse;

public class HttpClientExample {

    // TODO the HttpClient changed its interface and package, update this
    public static void main(String[] args) throws Exception{
//        HttpClient.getDefault()
//                .request(URI.create("http://www.quintor.nl"))
//                .GET()
//                .responseAsync() // CompletableFuture :D
//                .thenAccept(httpResponse -> System.out.println(httpResponse.body(HttpResponse.asString())));
//
//        Thread.sleep(5000); // The response is handled in a different thread. Give it some time to finish.
    }
}
